{% form_theme form 'tailwind_2_layout.html.twig' %}

{{ form_start(form) }}
    {{ form_widget(form) }}
<div class="flex">
    <button class="btn btn-outline btn-primary">{{ button_label|default('Créer') }}</button>
    {{ include('betting_group/_delete_form.html.twig') }}
</div>

{{ form_end(form) }}
