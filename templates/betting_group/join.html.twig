
{% extends 'base_front.html.twig' %}
{% form_theme form 'tailwind_2_layout.html.twig' %}
{% block title %}Rejoindre un groupe{% endblock %}

{% block body %}
    <div class="flex w-full bg-white flex-col justify-center items-center p-12 ">
                <h1>Rejoindre un groupe</h1>
                <p>Vous pouvez rejoindre un groupe en saisissant le code d'accès ci-dessous.</p>
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <button class="btn bg-blue-100">{{ button_label|default('Envoyer une requète') }}</button>
                {{ form_end(form) }}
    </div>
{% endblock %}
