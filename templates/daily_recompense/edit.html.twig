<!DOCTYPE html>

<title>Edit DailyRecompense</title>

{% block body %}
    <h1>Edit DailyRecompense</h1>

    {{ include('daily_recompense/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('front_daily_recompense_index') }}">back to list</a>

    {{ include('daily_recompense/_delete_form.html.twig') }}
{% endblock %}
