<form method="post" action="{{ path('app_group_request_delete', {'id': group_request.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ group_request.id) }}">
    <button class="btn">Delete</button>
</form>
